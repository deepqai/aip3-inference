# folder containing the images to process
IMG_FOLDER = "image"

# (optional) file holding the labels for the dataset (required for train, optional for inference)
LABEL_FILE = "label.csv"

# file holding the classes of interest (required to exist for inference)
CLASSES_FILE = "label_name.csv"

# folder containing the data insight folders
DATA_INSIGHT_FOLDER = "data_insight"

NO_ANNOTATION_LABEL = "nothing to label"
